# 中国国家发展模拟器程序维护与扩展指南

## 1. 文档目的

本文档面向维护本项目的程序员和 AI 编程代理，说明如何安全地修改现有模块、增加新模块、扩展状态和存档、添加国策与历史事件、接入界面，并完成相应测试、校准和审计。

本文档描述的是当前仓库的实际架构。开始修改前应依次阅读：

1. `AGENTS.md`：强制开发规范、验证要求和 Git 规则。
2. `README.md`：当前已经实现的玩法、数据口径和校准结果。
3. `开发文档.md`：完整产品需求和早期系统设计。
4. `经济模拟算法设计.md`：经济变量、库存流量和反馈回路设计。
5. 本文档：具体维护和扩展步骤。

当本文档与代码不一致时，以通过测试的当前代码为准，同时应在同一次修改中更新本文档。

---

## 2. 绝对不可破坏的系统约束

任何功能在进入主分支前都必须满足以下约束。

### 2.1 确定性

- 相同种子、相同初始状态、相同命令序列必须得到逐字段一致的结果。
- 模拟核心禁止使用 `Math.random()`、`Date.now()`、当前系统时间和未序列化的全局变量。
- 人口波动和随机事件分别使用可序列化随机流；增加新随机流时必须写入 `GameState` 和存档迁移。
- 日志、UI 刷新频率、批量运行方式不得改变随机数消耗顺序。
- 对集合进行结算时使用稳定顺序，不依赖对象枚举、数据库返回顺序或本地化排序的偶然结果。

### 2.2 核心与界面隔离

- `src/simulation/` 不得依赖 React、DOM、`window`、IndexedDB 或浏览器 Worker。
- React 只负责展示和发送 `SimulationCommand`，不得重复计算 GDP、人口、财政、科技、排名或政策收益。
- 模拟核心必须能由 Node.js 中的无界面工具直接运行。

### 2.3 数值安全

- 1949 年至当前年份连续运行不得出现 `NaN`、`Infinity`、负人口、负产出或无意义的比例。
- `clamp` 只用于有经济含义的上下界，不能用来掩盖单位错误或结算顺序错误。
- 所有除法优先使用 `safeDivide`，并为分母为零定义明确语义。
- 年率转月率、年度流量转月度流量时必须显式换算。

### 2.4 经济因果

- 国策和事件不得直接给 GDP、人口、世界排名或综合评分加固定值。
- 影响必须经过资本、劳动、生产率、教育、科研、贸易、财政、资源或制度等中间变量传导。
- 强收益必须存在财政、能源、通胀、外需、分配、外交或机会成本。
- 反事实路线从自己的资本、人才、技术和制度存量继续复利，不得在后续年份强行拉回史实 GDP。

### 2.5 存档连续性

- 导入存档后继续运行的结果必须与不中断运行完全一致。
- 对外导出的状态必须是深拷贝，不能泄漏引擎内部可变对象。
- 增加状态字段时必须给旧存档提供确定的默认值或重建规则。

---

## 3. 仓库结构与模块职责

```text
app/
├── simulator-dashboard.tsx       React 主界面和各功能页面
└── globals.css                   全局样式与响应式布局

src/
├── data/config/                  数据驱动配置和校准锚点
├── simulation/
│   ├── core/                     命令、引擎、随机数、时间和月度管线
│   ├── state/                    游戏状态、世界状态、历史快照和初始状态
│   ├── population/               人口与劳动力
│   ├── economy/                  资本、生产、GDP、贸易、外汇和通胀
│   ├── fiscal/                   财政收入、支出和债务
│   ├── society/                  教育、医疗和民生
│   ├── technology/               科研产出和科技树
│   ├── policies/                 常驻国策和发展路线蓝图
│   ├── events/                   随机事件、历史事件、决策和主动国策
│   ├── diplomacy/                双边关系、战略和国际组织
│   ├── world/                    世界国家演化和排名
│   ├── reports/                  月度、年度报告和历史比较
│   └── save/                     存档封装、校验和迁移入口
├── worker/                       Web Worker 适配层
└── ui/                           Worker 客户端、Zustand 和自动存档

tools/
├── baseline-calibration/         无界面路线、CSV、校准和批量模拟
└── final-audit/                  确定性、性能、极端路线和架构审计

tests/                            生产构建后的界面输出测试
```

职责边界：

| 需求 | 应修改的位置 | 不应修改的位置 |
| --- | --- | --- |
| 调整数值参数 | `src/data/config/*.json` | React 组件 |
| 增加经济公式 | `src/simulation/` 对应模块 | Zustand、Worker |
| 增加玩家操作 | `core/commands.ts`、`core/engine.ts`、UI Store | 直接改 `GameState` |
| 增加页面 | `app/simulator-dashboard.tsx`、`app/globals.css` | 复制模拟公式 |
| 增加历史锚点 | `calibration-targets.json` 及校准测试 | 在公式中硬编码年份结果 |
| 增加批量路线 | `tools/baseline-calibration/strategies.ts` | UI 状态 |

---

## 4. 从命令到界面的数据流

```text
玩家操作
  → Zustand action
  → SimulationCommand
  → SimulationWorkerClient
  → simulation.worker.ts
  → SimulationEngine.dispatch
  → simulateMonth（需要推进时间时）
  → 深拷贝 SimulationResult
  → Zustand game 状态
  → React 展示
  → IndexedDB 自动存档
```

关键文件：

- `src/simulation/core/commands.ts`：所有允许从外部改变模拟状态的命令。
- `src/simulation/core/engine.ts`：命令校验与分发入口。
- `src/simulation/core/protocol.ts`：Worker 请求和响应协议。
- `src/worker/simulation.worker.ts`：只做消息桥接和错误封装。
- `src/ui/simulation-client.ts`：请求编号和 Promise 管理。
- `src/ui/simulation-store.ts`：忙碌状态、错误提示、自动存档和界面操作。

如果某个新功能需要玩家主动操作，必须沿整条链路接入，不能让组件直接修改 `game`。

---

## 5. 月度结算管线

`src/simulation/core/month-pipeline.ts` 是唯一月度结算顺序入口。当前顺序如下：

1. 检查固定日期历史事件。
2. 如果存在待决历史事件，暂停且不推进月份。
3. 检查随机事件。
4. 推进国策过渡和制度、开放环境。
5. 更新外交关系与外交战略。
6. 检查自动取得的国际组织资格。
7. 更新人口出生、死亡和年龄结构。
8. 更新教育存量和培养队列。
9. 更新医疗。
10. 更新劳动参与和失业。
11. 更新科研、技术能力和科技树。
12. 更新资本、折旧和投资。
13. 分配三次产业劳动力。
14. 更新粮食和能源供给。
15. 计算产业产出和工业细分类别。
16. 汇总 GDP、收入、消费和储蓄。
17. 计算国际贸易。
18. 计算外汇、侨汇、资本品用汇和外债。
19. 计算财政收入。
20. 计算财政支出。
21. 结算政府债务。
22. 更新通胀和利率反馈。
23. 更新幸福度、贫困和社会状态。
24. 模拟世界国家。
25. 年末计算世界排名。
26. 记录月度和年度历史。
27. 推进并清理 Modifier。
28. 日期推进一个月。

### 5.1 决定插入位置的方法

新增模块前必须回答：

- 它读取的是本月期初库存、当月流量还是月末库存？
- 它的产出应在同月被哪些模块消费？
- 它是否会形成下月继续存在的库存？
- 它与历史记录之间是记录前还是记录后结算？
- 它是否可能在事件暂停月份重复执行？

例如，产业升级需要当月科研结果，所以科技在生产之前更新；贸易需要当月 GDP 和产业产出，所以贸易在 GDP 之后更新；外汇需要当月贸易结果，所以外汇在贸易之后更新。

不要仅为让测试通过而随意移动结算顺序。顺序变化通常会改变全部历史路线。

---

## 6. 状态、库存和流量

### 6.1 状态定义

- 国家状态：`src/simulation/state/game-state.ts`
- 世界状态：`src/simulation/state/world-state.ts`
- 历史快照：`src/simulation/state/history-state.ts`
- 初始值：`src/simulation/state/initial-state.ts`

### 6.2 常见库存

- 总人口和年龄组人口
- 资本存量、政府债务、外债和外汇储备
- 教育覆盖率、科研人才和人才代际缺口
- 科技指数、科研点数和已完成科技节点
- 基础设施、制度效率和国策进度
- 双边关系、外交点数和国际组织资格

### 6.3 常见流量

- 月度出生和死亡
- 当月投资和折旧
- 当月财政收入、支出和利息
- 当月出口、进口、侨汇和偿债
- 月度科研产出
- 年度报告中的 GDP 增长和人口增长

不能把年度流量连续十二个月完整累加。例如年利率应转换为月利率，年度死亡率应除以 12 后再作用于当月人口。

### 6.4 名义值与实际值

- 实际 GDP 用于生产能力、真实增长和内部历史口径。
- 名义 GDP 受价格水平影响，用于财政、债务率和部分资金流量。
- 当年价人民币和现价美元人均 GDP 由历史统计折算模块生成，只用于显示、比较和排名。
- 不同货币、价格年份和实际/名义口径不得直接相加。

### 6.5 国民经济账户与投入产出守恒

`src/simulation/economy/national-accounts.ts` 在每月生产、贸易和财政流量已经形成后编制账户，结果供下一月中间投入约束使用。14 类产品及直接消耗系数集中配置在 `src/data/config/input-output.json`。

扩展产品账户时必须同时完成：产品 ID 类型、空账户、旧存档重建、总产出和增加值映射、进口与出口分配、中间投入系数、最终使用权重、年度快照、无界面状态验证和界面中文名称。每类产品必须满足“国内产出 + 进口 = 中间使用 + 最终使用 + 出口 + 存货变化”，三种 GDP 口径的相对误差不得超过 `1e-8`。统计调和只用于解释现有口径差，不得反向修改生产结果来掩盖公式错误。

### 6.6 部门价格、工资与库存周期

`src/simulation/economy/market-dynamics.ts` 必须在通胀和国民账户结算之后运行，产出的库存约束只能由下一月读取。价格和库存产品 ID 与投入产出表共用，直接消耗系数通过 `inputCoefficientsForProduct` 读取，禁止再维护一份重复矩阵。

- 产品价格由宏观通胀、库存相对目标的需求压力、投入品相对成本和工资成本逐步调整。
- CPI 与 PPI 使用配置权重的几何平均，GDP 平减指数保持与名义/实际 GDP 口径一致。
- 年度化存货变化进入存量前必须除以 12，目标库存用国内可用供给和目标月数计算。
- 只有农业和工业实物商品适用过量库存生产约束；服务产出不可跨期储存。
- 工资指数由国民账户劳动报酬和就业人数编制，实际工资必须用 CPI 折算。
- 新增产品时同步更新目标库存月数、CPI/PPI 权重、迁移、验证、界面和正反向测试。

### 6.7 年龄性别队列、家庭与城乡迁移

`src/simulation/population/demographic-cohorts.ts` 在三段人口结算之后推进 18 个五岁年龄组。出生进入 0—4 岁组，死亡按年龄风险和组内人口分配，除 85 岁以上组外每组每月有六十分之一幸存人口进入下一组。随后分别把少儿、劳动年龄和老年队列调和到现有历史校准总量，因此新增细分不得产生第二套总人口。

调整年龄组时必须同步修改 `AgeBandId`、配置、固定 ID 顺序、初始分布、三段人口映射、界面标签、旧存档重建和无界面守恒测试。家庭规模是渐进库存，不得随单月 GDP 跳变；城乡迁移必须先按总人口增长推算“无迁移城市人口”，差额才记为迁移流量，不能把自然增长重复算成进城人口。

### 6.8 企业与所有制账户

`src/simulation/economy/enterprise-sectors.ts` 将企业部门分为五种所有制，份额按制度能力逐月收敛。增加值只取生产法 GDP 中的企业部门份额，再分别按生产率、融资、出口倾向和盈利能力分配就业、投资、出口与营业盈余。四项分配都必须与宏观总量守恒，不得把所有制优势再次作为无成本产出相加。

新增所有制或修改结构时同步更新 `EnterpriseOwnershipId`、配置、空账户、目标份额、分配权重、存档迁移、界面和审计。财政、信贷等后续模块应直接读取该账户上一月结果，不能在各模块内另行猜测国企或民企占比。

### 6.9 中央地方财政与社会保障

`src/simulation/fiscal/fiscal-federalism.ts` 只拆分现有合并财政。中央净收入加地方收入必须等于财政总收入，中央直接支出加地方支出必须等于总支出，中央债务加地方债务必须等于政府债务。中央对地方转移支付同时从中央收入扣除并计入地方收入，不得再次计入合并收入或支出。

五项社会保障的缴费和待遇从既有财政收入与福利预算中分配。新增项目时必须同步调整权重且保持和为 1，并补充覆盖人口、旧存档、守恒审计和界面说明。

---

## 7. Modifier 和国策修正机制

### 7.1 Modifier 的工作方式

事件修正存放在 `nation.modifiers`，由 `applyModifiers` 消费。相同目标按 `id` 稳定排序，然后依次执行：

- `add`：加到当前值。
- `multiply`：乘以倍率。
- `override`：覆盖当前值，应谨慎使用。

延迟修正在 `delayMonths` 归零后生效；`remainingMonths` 归零后移除。

最重要的规则：配置中出现一个 `target` 并不表示它会自动生效。必须在某个算法模块中存在：

```ts
const adjusted = applyModifiers(nation, "module.variable", baseValue);
```

新增 target 时先使用 `rg 'module.variable' src` 确认消费者。如果没有消费者，应在对应模拟模块接入并补测试。

### 7.2 常驻国策

常驻国策配置位于 `src/data/config/national-policies.json`，运行逻辑位于 `src/simulation/policies/policy-engine.ts`。

国策通过 `transitionMonths` 渐进达到完整强度。取消国策后，进度也会逐月下降，而不是瞬间归零。

增加国策的步骤：

1. 在 JSON 中增加唯一且稳定的 `id`。
2. 指定类别、说明、生效月数和互斥国策。
3. 为收益选择已有的中间变量 target。
4. 至少增加一个真实代价 target。
5. 如果新增 target，在算法中增加消费者。
6. 在 `policy-engine.test.ts` 增加正向、代价和互斥测试。
7. 检查国策页面是否能自动展示；特殊说明需要补充中文映射。
8. 运行批量路线，检查是否形成单一最优解。

示例：

```json
{
  "id": "example_policy",
  "name": "示例国策",
  "category": "发展",
  "description": "通过教育和设备投资改善产业能力，同时增加财政和能源压力。",
  "transitionMonths": 48,
  "conflictsWith": ["austerity"],
  "modifiers": [
    { "target": "education.efficiency", "operation": "multiply", "value": 1.08 },
    { "target": "capital.investmentEfficiency", "operation": "multiply", "value": 1.04 },
    { "target": "fiscal.spending", "operation": "multiply", "value": 1.025 },
    { "target": "resources.energyDemand", "operation": "multiply", "value": 1.02 }
  ]
}
```

不要使用 `economy.realGDP` 作为 target 直接奖励 GDP。

---

## 8. 增加或修改模拟状态字段

状态字段是风险最高的修改之一。必须完成以下清单。

1. 在 `game-state.ts` 或 `world-state.ts` 增加严格类型。
2. 在 `initial-state.ts` 增加初始值。
3. 编写 `ensureXxxState`，为旧存档补齐确定默认值。
4. 在引擎构造、`IMPORT_GAME` 和 `deserializeGameState` 路径调用迁移函数。
5. 如果年度比较需要该字段，在 `history-state.ts` 和 `reports/history.ts` 增加快照。
6. 如果 Worker 或 UI 需要操作，更新命令、协议和 Store。
7. 如果界面需要展示，更新组件和生产构建测试。
8. 增加旧存档迁移测试。
9. 增加存档后继续运行与不中断运行一致的测试或审计。

迁移函数应满足幂等性：连续调用两次不能改变已经完整的当前状态。

推荐形式：

```ts
export function ensureExampleState(nation: NationState): void {
  nation.example ??= {
    stock: 0,
    efficiency: 1,
  };
  nation.example.stock = Number.isFinite(nation.example.stock)
    ? Math.max(0, nation.example.stock)
    : 0;
}
```

不要通过不安全类型断言让缺失字段“看起来存在”。

### 8.1 何时提升存档版本

只有以下情况才考虑修改 `SAVE_SCHEMA_VERSION`：

- 无法为旧结构提供确定迁移。
- 字段语义发生不可逆变化，旧值无法可靠解释。
- 存档封装或校验算法发生不兼容变化。

普通新增字段优先保持版本不变并迁移。

当前开发期的存档结构版本为 `2`。义务教育和证券交易所迁移为历史转折国策时，项目尚无需要保留的旧存档，因此没有为旧国策 ID 增加别名或数据迁移；版本 `1` 存档会被明确拒绝。项目对外发布并产生正式存档后，后续版本必须重新遵循上述兼容原则。

---

## 9. 增加新的核心模拟模块

假设要增加 `transport` 系统：

1. 明确目标和因果链，例如基建投入 → 运输能力库存 → 物流瓶颈 → 工业与贸易。
2. 在 `state` 中增加运输状态和迁移函数。
3. 在 `src/data/config/transport.json` 放置可校准参数。
4. 在 `src/simulation/economy/transport.ts` 实现纯函数式月度更新。
5. 明确库存、流量、单位、上下界和滞后。
6. 将更新函数插入 `month-pipeline.ts` 的正确位置。
7. 从 `src/simulation/index.ts` 导出公共 API。
8. 增加模块单元测试：正常路线、资源不足、极端输入和长期边界。
9. 增加审计，证明模块真的影响下游而不是只增加一个孤立数值。
10. 最后接入命令、Worker 和 React。

模块不得读取 UI 状态，不得在模块级保存可变单例。

---

## 10. 历史事件、玩家决策和主动国策

### 10.1 固定日期历史事件

定义位于 `src/data/config/historical-events.json`。每个事件至少包含：

- 唯一 `id`
- 年、月和类别
- 正面、负面或混合影响
- 中文说明和可见效果
- 默认持续时间
- 通过中间变量生效的 Modifier

史实路线由事件本体定义。不要把史实选择重复写入决策配置。

### 10.2 玩家可选方案

专属方案位于 `historical-event-decisions.json`。引擎会自动把史实路线作为第一个选项，再追加这里的方案。

每个方案必须说明：

- 是否阻止事件发生。
- 短期成本。
- 中长期收益或风险。
- 与史实路线不同的持续期。
- 对应的中间变量传导。

如果方案阻止事件，应使用 `"outcome": "prevented"`。时间线会显示“避免”。

### 10.3 事件因果依赖

`historical-event-dependencies.json` 用于让早期决策改变后续事件强度和持续期。例如避免大跃进和人民公社化会减轻三年经济困难。

三年困难（1959年1月）在史实继续出口之外，备选顺序优先展示粮食贸易取向：`limit_grain_exports`（约对半削减 1959—1960 年 416/272 万吨量级净出口）与 `ban_grain_exports_and_import`（停止高额净出口并部分前移 1961—1962 年 445/389 万吨量级净进口），其后才是国内赈济与接受外援。史实路径不得改写为“默认禁运”，以免破坏人口与经济校准；强度大致为史实出口 < 限制出口 < 国内赈济 < 禁止出口并进口 < 外援。新选项通过 `resources.foodSupply`、死亡率与资本品用汇等中间变量形成可测取舍。

只有存在明确因果关系时才增加依赖，不要把所有更优决策统一变成后续事件减伤。

专属外交决策示例：`albania_aid_after_soviet_cutoff_1961`（1961年3月）对应苏阿断援后中国是否对阿提供粮食、外汇与成套项目；史实路线写入事件本体，削减与拒绝写入 `historical-event-decisions.json`，不得做成常驻国策。可选 `foreignAidAdjustment` 在月度援外结算中叠加相对史实基线的年化人民币／外汇差额：`annualRmbDelta` 调整援助承诺总额，`annualForeignExchangeRmbDelta` 表示专属外汇强度（如文案约 2.5 亿元外汇援款，按三年期年化后乘 0.95 入账，避免与既有史实用汇完全重合），再叠加到 `annualForeignAidForeignExchangeOutflow`。外储只结算该强度相对事件史实路线基线的差额，避免史实路径重复扣减已校准外汇。不得只靠统一外汇比例覆盖事件声明的外汇量级。事件剩余月数递减（`tickForeignAidEventAdjustment`）必须排在当月 `updateForeignExchange` 之后，否则最后一月会提前清空史实专属外汇基线并额外扣减外储。削减／拒绝等备选方案的 `durationMonths` 必须与 `foreignAidAdjustment.durationMonths` 一致，避免国内 modifier 先到期而外汇账户仍在结算。

新增世界国家时：既有 31 国 ID 写死在 `legacySharedWorldCountryIds` 常量中并消耗共享随机流；**任何不在该列表中的新国家**（含阿尔巴尼亚及以后新增国）使用按种子＋国家 ID＋年月派生的独立流。不得用“排除某个 id”的动态过滤生成该列表。`ensureWorldCountriesState` 补齐后必须按配置顺序重排。

若早期决策通过 `historical-event-dependencies.json` 缩放事件强度，史实路径与备选方案的 modifier、持续期以及 `foreignAidAdjustment` 金额／持续期一并缩放；外储史实基线取缩放后的史实专属外汇强度。

### 10.4 可提前发动的历史转折

`historical-event-initiatives.json` 用于政府可主动提前推动的改革、建设或国际合作。

主动国策必须具有：

- 最早年份。
- 前置历史事件及等待月数。
- 制度、稳定、开放、教育、科技或产业门槛。
- 所有制与企业制度改革可按需检查民营经营空间和企业家组织能力门槛。
- 外交类行动的关系、支持国家、协定和外交点成本。
- 过渡期收益与代价。

战争、灾害、经济危机和政治运动不应改成无条件主动收益按钮。

### 10.5 历史事件测试

至少验证：

- 正确年月只触发一次。
- 交互模式会暂停月份。
- 选择被写入历史记录。
- 至少三个方案产生不同中间状态。
- 阻止路线不携带史实负面 Modifier。
- 延迟 Modifier 在正确月份开始和结束。
- 长期库存差异在 Modifier 到期后仍然存在。

---

## 11. 随机事件

随机事件配置位于 `src/data/config/events.json`，逻辑位于 `event-engine.ts`。

增加随机事件时：

1. 使用项目随机流，不得自行调用随机函数。
2. 概率应与状态相关或至少有明确年度数量级。
3. 遵守每年事件数量限制。
4. 使用 Modifier 影响中间变量。
5. 确保事件名称进入年度报告。
6. 增加相同种子结果一致、不同种子可能分化的测试。

不要让新增日志或 UI 预览额外消耗随机数。

---

## 12. 科技树和工业类别

### 12.1 增加科技节点

科技树配置位于 `technology-tree.json`，逻辑位于 `technology/technology-tree.ts`。

增加节点必须填写：

- 稳定 `id` 和中文名称。
- 所属科技分支。
- 科研成本。
- 教育和科技指数门槛。
- 前置节点。
- 产业层级。
- 玩家可见效果。
- 对一个或多个工业类别的生产率和出口倍率。

检查事项：

- 前置节点必须存在且不得形成环。
- 旧节点 ID 不得随意改名，否则旧存档会丢失完成记录。
- 科研成本应让高投入路线明显更快，但不能一月完成整棵树。
- 专业技术只强化相关工业，不能让冶金节点直接提高电子工业。
- 新节点应在科技页面显示，并通过 UI 构建测试。

### 12.2 增加工业类别

工业配置位于 `industrial-categories.json`，状态 ID 联合类型位于 `game-state.ts`，逻辑位于 `economy/industrial-structure.ts`。

增加类别需要同步修改：

1. `IndustrialCategoryId`。
2. `createInitialIndustrialCategories`。
3. 配置中的基准份额。
4. 高技术类别集合（如适用）。
5. 科技节点的 `industrialEffects`。
6. 工业页面和测试。

所有 `baselineShare` 之和必须严格等于 1。

工业类别的 `valueAdded` 之和必须等于第二产业增加值；类别只是拆分第二产业，不能再次加入 GDP。工业品出口之和不得超过总出口。

### 12.3 科技向工业的传导

当前传导链为：

```text
教育与科研预算
  → 科研人才和月度科研产出
  → 科技树节点
  → 类别技术准备度与节点专属倍率
  → 工业复杂度、类别份额和出口能力
  → 第二产业产出与国际贸易
```

高复杂度产出还要求开放市场和产业升级组织能力，避免只有抽象科技指数就自动获得全部经济收益。

### 12.4 增加或调整科技工业发展路线

长期路线配置位于 `src/data/config/technology-industry-paths.json`，状态与计算位于 `technology/technology-industry-path.ts`。路线选择通过 `SET_TECHNOLOGY_INDUSTRY_PATH` 命令进入引擎，React 只能展示定义和派发命令，不得复制路线公式。

每条路线必须配置：

- 稳定 `id`、中文名称、简称和说明。
- 优先科技节点或优先科技分支。
- 专项与非专项科研倍率。
- 能源需求倍率。
- 所影响工业类别的目标份额、生产率和出口倍率。
- 至少一项玩家可见收益和一项可感知的机会成本；综合路线可作为中性基准。

月度传导链为：

```text
玩家选择发展路线
  → 当前研究进度付出重定向损失并开始冷却
  → 48 个月路线过渡
  → 自动研究优先级与节点科研效率
  → 类别目标份额、生产率、能源需求和出口倍率
  → 产业结构、工业产出、出口能力、贸易和后续资本积累
```

发展路线与单个研究目标是两个不同维度。路线影响长期资源配置，手动目标只选择当前研究节点；路线外研究必须允许执行，但适用非专项效率。任何路线都不得跳过前置科技、教育门槛或科技能力门槛，也不得直接修改 GDP、出口额和排名。

新增或修改路线时必须同步检查：

1. `TechnologyState["developmentPathId"]` 联合类型与初始状态。
2. 旧存档迁移默认值、切换后的存档连续性和确定性。
3. 自动研究优先级是否在相同种子下稳定。
4. 工业目标份额归一化后，十一类增加值之和是否仍等于第二产业。
5. 路线的正向产业、受损产业、能源代价与路线外科研代价测试。
6. 科技页选择卡、工业页实时倍率和生产构建内容断言。
7. 史实路线是否保持“综合基础体系”的中性基线，历史校准是否发生意外偏移。

### 12.5 内外需求与产能利用

`domestic-demand.ts` 负责把上一结算月已经实现的居民消费、投资、政府公共服务支出、出口和进口转换为各产业的产能利用率目标，参数集中在 `domestic-demand.json`。传导顺序为：

```text
社会保障、税后收入和侨汇
  → 居民可支配收入、消费倾向和国内储蓄
  → 内需规模
科技树、工业结构和外交市场准入
  → 已实现出口
内需与出口
  → 下一月产能利用率
  → 产业增加值与 GDP
```

出口和财政转移不得直接加入 GDP。需求只改变已有资本、劳动和技术能力的实际利用程度，而且应采用滞后、渐进和有界的调整；供给能力不足时，旺盛需求应更多表现为通胀、进口和短缺，而不是无限增产。

社会保障属于收入再分配。新增机制时要区分社会保障支出、居民收到的有效转移收入和消费倾向变化，避免把同一笔财政支出同时计入政府消费、居民消费和资本形成。修改需求参数后必须同时检查史实校准、产业结构、通胀、储蓄投资以及极端高福利和高出口路线。

### 12.6 增加或调整产业政策

产业政策配置位于 `src/data/config/industrial-policies.json`，状态和计算位于 `src/simulation/policies/industrial-policy.ts`。玩家操作必须通过 `SET_INDUSTRIAL_POLICY` 命令进入引擎；React 工业页面只展示状态、实时倍率、调整冷却和派发命令，不得复制效果公式。

配置由三部分组成：

- `transitionMonths` 与 `minimumChangeIntervalMonths`：扶持、退出、限制的过渡期和调整冷却。
- `stances`：产出权重、生产率、投资、出口、科研、能源、财政成本、信贷、错配和就业方向。
- `categoryParameters`：每一工业类别的扶持成本、就业敏感度和供应链重要性。

新增工业类别时，除了 12.2 节所列项目，还必须在产业政策配置中补齐类别参数，否则配置验证和最终审计应失败。调整参数时不得只验证目标行业收益，还要同时检查：

1. 扶持是否进入财政支出，并形成信贷集中与错配代价。
2. 扶持收益是否受到预算、科技准备度和行政能力约束。
3. 同时扶持多个行业时，行政执行有效性是否下降。
4. 限制是否压低目标行业的投资、产出权重和出口。
5. 限制就业密集行业是否提高失业压力，限制关键上游是否形成供应链约束。
6. 十一类增加值是否仍等于第二产业增加值，出口是否仍受总出口上限约束。
7. 全部中性时是否走快速路径，并保持历史路线 85/85 校准项不变。

月度顺序是：国策与制度环境更新后推进产业政策强度和代价，随后外交、人口、劳动力、科研、资本、生产、产业结构、财政和金融模块消费这些中间量。不要把产业政策挪到生产之后再同月回写产出，否则会造成时点循环。

产业政策状态由初始状态创建，在引擎创建、导入和存档反序列化边界调用 `ensureIndustrialPolicyState` 规范化。热路径中的 `industrialPolicyEffect` 不应反复执行完整迁移校验；全部中性时必须直接返回中性聚合效果，否则 1949—当前年份的批量运行会产生明显性能回归。

最低测试集合包括 `industrial-policy.test.ts` 的扶持正向案例、限制反向案例、多行业行政稀释、命令冷却与深拷贝，以及最终审计的 `targeted-industrial-policy` 对照。界面文字和操作入口由 `rendered-html.test.mjs` 验证。

---

## 13. 外交、国家和国际组织

### 13.1 世界国家

国家配置位于 `world-countries.json`。增加国家时检查：

- 初始经济规模、人口、教育、科技和发展阶段。
- 外交初始关系是否在 `diplomacy.json` 中有定义。
- 世界排名和双边关系 UI 是否能自动展示。
- 该国家是否应进入特定历史事件或援助来源列表。
- 引擎创建、存档导入与反序列化会调用 `ensureWorldCountriesState`，按配置补齐缺失国家后再跑 `ensureWorldTradeNetworkState`；关系 Modifier 依赖 `diplomacy.relationTarget.<countryId>`，缺少国家会导致加成落空。

### 13.2 外交行动

外交行动配置位于 `diplomacy.json`。行动通常包括：

- 外交点成本。
- 最低关系。
- 冷却期。
- 关系变化。
- 协定、伙伴、制裁等状态变化。

新行动需要更新 `DiplomaticActionId` 的推导结果、执行分支和正反反馈测试。

### 13.3 阵营取向与外交学说

外交采用两个相互独立、可以组合的决策层：

- 亲苏、平衡、亲西方属于阵营取向，位于 `diplomatic-strategies.json` 和 `diplomatic-strategy.ts`。
- 和平共处、战略自主、经贸优先、多边主义等属于对外行为学说，位于 `foreign-policy-doctrines.json` 和 `foreign-policy-doctrine.ts`。

不要把两层合并为单一枚举。玩家应能同时选择“亲苏 + 和平共处”或“亲西方 + 战略自主”，两层效果再在贸易、科研和外交模块中相乘或相加。增加外交学说时必须定义调整成本、过渡期、冷却期、收益与代价，并至少覆盖以下一种传导：

- 国家组与具体国家的关系目标调整。
- 市场准入、外资信心或技术扩散倍率。
- 科研产出、安全目标、国际声誉或外交点恢复。

国家组只在 `foreign-policy-doctrines.json` 中维护，避免把“苏系”“非苏系”“区域邻国”等列表散落到算法和 React 组件。具体国家调整与国家组调整可以叠加，用于表达韩国、朝鲜等特殊关系，但测试必须验证方向和最终合计。

新增学说还要同步检查：

1. `DiplomacyState` 的字面量联合类型和初始值。
2. `ensureDiplomacyState` 的旧存档迁移默认值。
3. `SimulationCommand`、引擎 `dispatch` 和 Zustand 操作。
4. 外交页面的成本、冷却、过渡进度和不可执行原因。
5. 贸易、外资、科研、技术、安全、声誉与外交点的正反反馈测试。
6. 最终审计中的路线差异和 1949 年至当前年份连续运行。

阵营取向和外交学说都不得直接增加 GDP、出口额或世界排名。切换时保留前一学说并按进度插值，避免关系和经济倍率在单月瞬间跳变。

### 13.4 对外援助方案

对外援助配置位于 `foreign-aid-programs.json`，核心逻辑位于 `diplomacy/foreign-aid.ts`。它与阵营取向、外交学说相互独立：外交学说描述行为原则，援助方案描述实际投入规模、援助形式和受援对象。

配置援助方案时必须定义：

- 史实规模倍率和 1980 年后的当年价 GDP 占比。
- 财政占比和需要直接使用外汇的比例。
- 重点受援国 ID；不得在 React 中另建国家组。
- 关系、声誉和外交点反馈。
- 国内投资、科研产出、工业生产率和出口网络倍率。
- 调整成本、收益、国内机会成本和玩家可见说明。

`historicalSchedule` 使用当年价人民币，`officialExchangeRateAnchors` 使用人民币/美元官方汇率。禁止拿游戏内不变价 GDP 直接与当年价援助金额相加。累计人民币与美元仅作历史金额和外汇流量记录，经济影响按相对史实基线的中间变量传导：

```text
援助规模与受援方向
  → 设备物资、科研人员和援外用汇
  → 国内资本形成、工业生产率和科研产出
  → 出口网络、外汇储备和后续产业升级
  → 受援国关系目标、国际声誉和外交点
  → 市场准入、贸易、外资与国际组织支持
```

史实综合援外的国内经济倍率均为 1，因为现有历史校准已经隐含该成本。停止援助或扩大援助必须计算相对史实基线的资源释放或额外挤出，不能再次扣除史实成本。`foreignAidExpenditure` 与年度承诺同口径，只是财政归因展示项，**不得**叠加进 `fiscal.expenditure`；真代价走国内倍率与相对外储流量。强政策（扩大援助、经贸合作）必须相对史实保留可测国内代价。

修改援助模块时同步检查：

1. `DiplomacyState`、`FiscalState`、初始状态和旧存档迁移。
2. `SET_FOREIGN_AID_PROGRAM`、引擎、Worker、Zustand 和外交页面。
3. 1950—1980 年史实累计人民币与官方汇率美元等值。
4. 受援国关系提高、非受援国不获得定向关系收益。
5. 暂停援助的国内资源收益与外交、声誉、出口网络代价。
6. 扩大援助或集中伙伴援助的财政、外汇、投资和科研挤出；经贸合作相对史实的国内机会成本。
7. 年度快照、确定性、历史校准和最终审计。

### 13.5 中美建交外交进程

中美建交配置位于 `sino-us-normalization.json`，核心状态机位于 `diplomacy/sino-us-normalization.ts`。它不是可重复切换的外交学说，也不是到年份后无条件发放的增益，而是只有一次的“未启动 → 谈判中 → 已建交”进程。

维护时必须保留以下口径：

- 史实自动路线在 1979 年 1 月建交，作为既有历史经济校准的中性时间基线。
- 交互路线必须由玩家发动，检查对美关系、声誉、稳定、制度效率、制裁状态和外交点。
- 谈判月数由启动时的对美关系确定并写入存档，推进中不得根据后来状态反复重算。
- 提前或延迟影响使用“实际合作成熟度－史实合作成熟度”的边际倍率；史实路线不能重复获得一套已经隐含在校准中的经济收益。
- 教育吸收能力、开放度和制度效率共同决定科技、留学与外资收益，低能力国家不能因提前建交瞬间获得完整先进技术。
- 美国、日本、韩国、俄罗斯和朝鲜的关系只修改目标值，再由外交系统逐月收敛。
- 建交后的对美贸易协定有单独滞后，不能在谈判启动当月立即获得完整市场准入。

传导链为：

```text
关系、声誉、稳定、制度与外交资源
  → 建交谈判时间
  → 正式建交与使领馆渠道
  → 留学生、科研合作、技术设备、贸易协定与外资信心
  → 教育人才、科技扩散、出口客户和资本存量
  → 工业升级、贸易、外汇和后续增长
```

延迟建交不应直接扣除 GDP。未形成的留学人才、技术扩散、出口和外资流量自然导致较低存量，后来完成建交只恢复未来流量，不追溯补发过去收益。新增字段时同步检查 `DiplomacyState`、初始状态、`ensureSinoUSNormalizationState`、年度快照、命令、Zustand、外交页面和旧存档迁移。测试至少覆盖提前、史实、延迟、对苏关系代价、贸易协定滞后、教育科技出口传导及存档连续性。

### 13.6 国际组织

联合国席位和 WTO 等资格由条件自动触发，不是普通国策。增加组织时应定义：

- 最早年份。
- 影响力和开放度。
- 平均国际关系。
- 支持国家数量与关系门槛。
- 贸易协定数量。
- 前置历史进程和最少谈判月数。

自动组织不应在 UI 提供绕过条件的加入按钮。

---

## 14. 财政、外汇和债务扩展

修改财政或外汇时必须保持闭环。

### 14.1 财政闭环

```text
税基与税率
  → 财政收入
  → 各项支出和利息
  → 赤字或盈余
  → 政府债务
  → 风险利率和利息支出
  → 下月财政空间与通胀
```

增加支出项目时应同步修改 `FiscalBudget`、初始预算、预算 UI、总额校验、财政结算和测试。

### 14.2 外汇闭环

```text
出口、外资和侨汇
  → 可用外汇
  → 进口与资本品用汇
  → 外汇储备变化
  → 资本形成和生产能力
```

外债必须区分借入、用途、计息、偿还和未付利息资本化。生产性外债不能同时作为一般储备和资本品融资重复使用。

---

## 15. 增加玩家命令和 UI 功能

### 15.1 命令链路

增加 `SET_EXAMPLE` 命令时：

1. 在 `core/commands.ts` 增加命令接口并加入联合类型。
2. 在 `core/engine.ts` 增加分支、条件校验和状态更新。
3. 核心逻辑应调用领域模块，不要把公式写进 `switch`。
4. 在 `simulation-store.ts` 增加异步 action。
5. 组件调用 Store action，不直接改状态。
6. 增加命令测试和中文错误提示测试。

Worker 协议使用 `SimulationCommand` 联合类型，普通命令通常不需要重复定义一套消息格式。

### 15.2 新增页面

1. 在 `SectionId` 和菜单中增加页面标识。
2. 创建展示组件。
3. 数据只来自 `GameState` 或核心导出的只读计算函数。
4. 显示不可执行原因、成本、门槛、冷却和互斥关系。
5. 处理 `busy` 和错误状态。
6. 在 `globals.css` 中检查桌面和窄屏布局。
7. 在 `tests/rendered-html.test.mjs` 增加生产构建内容断言。

大型图表继续使用懒加载，避免首屏包体继续增大。

---

## 16. 历史记录、排名和比较数据

月度历史最多保留最近 120 个月；年度历史和年度报告长期保留。

增加年度指标时需要同步：

1. `MonthlySnapshot` 或 `AnnualSnapshot` 类型。
2. `recordHistory` 写入逻辑。
3. CSV 导出表头和内容。
4. UI 图表或统计表。
5. 旧存档年度记录迁移默认值。
6. 相关测试。

世界 GDP 排名由世界国家模型计算；全球人均 GDP 排名使用历史锚点和玩家相对史实收入变化计算。不要在 React 中自行排序得到另一套排名。

中国历史的用户界面当年价对比配置位于 `historical-current-price-targets.json`，人民币为主要口径、同年美元为辅助口径；内部不变价历史仍只用于校准与实际增长分析。国际比较配置位于 `comparison-economy-targets.json`，统一使用现价美元。两类比较都只用于展示，不得反馈到模拟状态。新增展示锚点时必须明确年份、人口、价格口径、币种和来源，不得把 `realGDPPerCapita` 直接标成普通“人均 GDP”。

---

## 17. 校准方法

### 17.1 校准来源

历史观测数据必须先登记到 `src/data/config/historical-data-registry.json`，再进入校准目标。每条主序列必须说明发布者、来源地址、单位、价格基准、适用范围、分时期可信度和口径备注；模拟机制参数不得伪装成观测数据写入注册表。修改后先运行 `npm run data:audit`，确保来源引用、指标唯一性、年份覆盖以及拟合、留出验证、预测三组完整。

校准时不得用留出验证年份调参。早期历史重建、国际排名和当前年份预测应保留较低可信度标签，不能通过缩小容差制造虚假精度。

- 历史锚点：`calibration-targets.json`
- 文革年度增长锚点：`cultural-revolution-impact-targets.json`
- 韩国追赶目标：`korean-catch-up-targets.json`
- 史实和反事实路线：`tools/baseline-calibration/strategies.ts`

配置中的来源应优先使用统计机构、政府档案、国际组织或研究论文，并记录 URL 和口径说明。

### 17.2 正确校准顺序

校准失败时依次检查：

1. 单位和价格口径。
2. 年率与月率换算。
3. 月度结算顺序。
4. 库存是否被当成流量重复累计。
5. Modifier 是否重复作用或根本没有消费者。
6. 历史事件月份、持续期和延迟期。
7. 资本、人口、科技等存量是否在修正到期时被错误倒扣。
8. 世界排名比较口径。
9. 最后才调整配置参数。

禁止仅通过扩大 `tolerances` 让校准通过。

### 17.3 反事实路线

史实路线必须匹配历史锚点。优化路线不应匹配史实，而应从更高资本、人才、开放度和生产率基数继续发展。

每次修改历史事件后至少比较：

- 史实路线。
- 只改变该事件的路线。
- 全部优化路线。
- 一个资源不足或极端政策路线。

---

## 18. 测试策略

### 18.1 测试层级

| 层级 | 目的 | 典型位置 |
| --- | --- | --- |
| 数学与工具测试 | 边界、确定性、单位 | `core/*.test.ts` |
| 模块测试 | 正向机制和负反馈 | 各领域同目录 `*.test.ts` |
| 引擎测试 | 命令、暂停、存档连续性 | `core/`、`events/` |
| 批量路线测试 | 长期稳定和路线分化 | `tools/baseline-calibration/` |
| 最终审计 | 架构、性能、校准、极端策略 | `tools/final-audit/` |
| UI 构建测试 | 生产包确实包含功能 | `tests/rendered-html.test.mjs` |

### 18.2 新机制的最低测试集合

每个新机制至少应有：

- 一个正常情况下产生预期收益的测试。
- 一个资源不足、能力不足或代价反馈测试。
- 一个长期运行边界测试。
- 如果有随机性，增加确定性测试。
- 如果增加状态字段，增加旧存档迁移测试。
- 如果改变历史路线，增加校准或审计断言。
- 如果显示在界面，增加生产构建测试。

### 18.3 完整验证命令

```powershell
$OutputEncoding = [Console]::OutputEncoding = [System.Text.UTF8Encoding]::new()
npm run typecheck
npm run lint
npm test
npm run calibrate
npm run audit
npm run build
npm run test:ui
git diff --check
```

修改纯文档时至少运行 `git diff --check`，并检查文档链接和命令是否对应当前仓库。

---

## 19. 常见故障排查

### 19.1 出现 NaN 或 Infinity

1. 从最终审计报告确定首个异常年份。
2. 缩小到首个异常月份。
3. 检查新增除法、幂运算和对数。
4. 检查 JSON 中字段缺失是否成为 `undefined`。
5. 检查旧存档迁移是否遗漏。
6. 不要先用 `clamp` 把异常变成有限数。

### 19.2 相同种子结果不一致

1. 搜索 `Math.random`、当前时间和可变单例。
2. 检查新增集合遍历顺序。
3. 检查是否按 UI 刷新次数调用随机数。
4. 检查随机流状态是否写入存档。
5. 对比两个引擎每月状态，找到首个分叉字段。

### 19.3 存档后继续运行不同

1. 对比导入前后完整状态。
2. 检查迁移函数是否修改了已经完整的新存档。
3. 检查队列、Modifier 延迟、随机流和研究进度是否序列化。
4. 检查引擎构造和 `IMPORT_GAME` 是否调用相同迁移集合。
5. 检查导出是否为深拷贝。

### 19.4 Modifier 看起来没有效果

1. `rg` 搜索 target 是否有 `applyModifiers` 或 `applyPolicyModifiers` 消费者。
2. 检查 `delayMonths` 是否仍大于零。
3. 检查 `remainingMonths` 是否已经归零。
4. 检查 target 字符串拼写和大小写。
5. 检查政策进度是否仍为零。
6. 检查产业升级收益是否被科技准备度门槛折减。

### 19.5 GDP 在某年突然下降

1. 检查临时产出倍率是否到期。
2. 检查先前收益是否错误存成临时倍率，而不是资本或生产率库存。
3. 检查价格口径是否切换。
4. 检查资本品外汇满足率、能源供给和债务利率。
5. 检查产业份额归一化和类别增加值合计。

### 19.6 校准通过但玩法失真

校准只证明史实路线接近锚点，不证明机制合理。继续检查：

- 避免重大负面事件是否产生足够长期收益。
- 极端举债是否出现通胀和利率反馈。
- 封闭路线是否无法获得完整出口和外资收益。
- 教育和科研是否存在真实滞后。
- 高科技工业是否需要对应节点和技能。
- 是否存在无代价、可叠加的单一最优国策。

---

## 20. 性能维护

无界面单路线 1949—当前年份应保持在审计预算内。

性能注意事项：

- 不在月度循环中反复解析 JSON。
- 可复用的定义表和 ID 集合在模块加载时建立。
- 避免每月对完整历史数组做多次全表扫描。
- 月度历史保持固定上限。
- 不为性能引入未序列化缓存；缓存若影响结果必须写入状态。
- 优化后必须证明逐字段结果不变。

性能变慢时分别测量单路线和 Vitest 并行运行，避免把测试并发资源竞争误判为核心算法退化。

---

## 21. Git 和分阶段提交

开始前：

```powershell
git status --short
```

保留用户已有改动，不提交无关文件。`dist/`、`outputs/`、`work/`、构建缓存和本地日志不得提交。

多阶段修改推荐顺序：

1. 状态和无界面核心。
2. 配置和数据。
3. 单元测试及迁移。
4. 批量模拟、校准和审计。
5. Worker、Store 和 React。
6. 文档。

如果每个阶段可以独立验证，应分别提交。提交信息使用简体中文，例如：

```text
阶段68：增加交通运输库存与结算
阶段69：接入交通政策和历史事件
阶段70：增加交通管理界面
```

提交前运行与该阶段风险相称的测试和 `git diff --check`。

---

## 22. AI 编程代理执行模板

AI 接到扩展任务后应按以下顺序执行。

### 22.1 调研

1. 阅读 `AGENTS.md` 和本文档。
2. 检查 `git status`。
3. 使用 `rg` 找出现有相似模块、状态字段、target 和测试。
4. 确认用户已有改动，不覆盖无关内容。
5. 涉及历史、法律、统计口径时查询可靠来源并记录来源。

### 22.2 设计

在修改前明确：

- 新增的库存和流量。
- 结算时点。
- 玩家操作入口。
- 收益、代价和滞后。
- Modifier target 及消费者。
- 存档迁移方案。
- 史实路线和反事实路线预期。
- 最低测试集合。

### 22.3 实现

1. 先实现无界面核心。
2. 再增加配置和数据。
3. 编写单元测试并运行局部测试。
4. 运行无界面路线和校准。
5. 最后接 Worker 和 React。
6. 补 UI 构建测试、审计和文档。

### 22.4 交付

交付说明必须包含：

- 实际完成的机制，而不是只列修改文件。
- 关键经济传导链。
- 存档兼容情况。
- 实际通过的测试数量。
- 校准通过数量和重要历史锚点。
- 审计周期和性能。
- 未执行的检查及原因。
- 提交哈希。

不要在没有运行验证时声称“全部通过”。

---

## 23. 完成定义

一项模拟功能只有同时满足以下条件才算完成：

- [ ] 无界面核心实现且不依赖浏览器。
- [ ] 月度结算位置明确。
- [ ] 数值参数集中在配置文件。
- [ ] 收益和代价通过中间变量传导。
- [ ] 同种子结果完全确定。
- [ ] 不产生非法数值。
- [ ] 新状态支持旧存档迁移。
- [ ] 存档连续性保持一致。
- [ ] 正向和负反馈测试齐全。
- [ ] 1949—当前年份连续运行。
- [ ] 史实校准没有通过放宽容差解决。
- [ ] 极端策略审计通过。
- [ ] UI 只展示和发送命令。
- [ ] 桌面端和窄屏可用。
- [ ] 文档、中文说明和数据来源已更新。
- [ ] `git diff --check` 通过。
- [ ] 按阶段完成提交。

满足以上清单后，功能才可以交付给玩家或继续部署。

---

## 24. 扩展货币金融模块

- 可校准参数集中在 `src/data/config/monetary-financial.json`。
- 银行账户在 `src/simulation/economy/monetary-financial.ts` 中只分配既有资产和融资，不可直接修改 GDP。
- 新增金融工具时必须同时说明发行人负债、持有人资产、计息周期、违约损失和资本占用。
- 新增跨境流量时必须选择经常账户或金融账户中的唯一位置，并重新核对储备变动恒等式。
- 修改 `FinancialSystemState` 后同步检查初始状态、引擎导入、存档反序列化、月度历史、批量验证、最终审计和国际页面。

## 25. 扩展农业与粮食模块

- 农业净产量仍由生产模块形成，农业细账不得再生成一份产量。
- 粮食进口、出口和储备统一使用吨/年流量；月度入库与释放必须除以 12 后改变库存。
- 新增灾害、配给或援粮决策时优先修改产量、进口能力、库存释放或分配效率，再让粮食保障率传导到人口和价格。
- 修改农业状态时同步维护旧存档重建、历史快照、批量守恒检查和农业页面。

## 26. 扩展能源运输与环境模块

- 新能源必须加入 `EnergySourceId`、初始账户、排放因子、份额归一化和界面名称。
- 能源细账只分配 `resources.energySupply`，不得与生产模块重复累计供给。
- 运输网络是库存，货运需求和港口吞吐是年度化流量；新增建设项目时必须按月增加网络库存。
- 环境成本应通过健康、农业、财政治理或生产率逐步传导，不得直接扣固定 GDP。

## 27. 扩展人力发展细账

- 新学段或技能层级必须同步更新联合类型、初始账户、守恒分配、界面和测试。
- 学段适龄人口不得与年龄队列另建不相容总量；技能就业必须调和到劳动力模块。
- 疾病负担是比例指标，健康预期寿命不得高于总预期寿命。
- 若把健康劳动损失接入生产，必须避免与现有健康指数对有效劳动的影响重复计算。

## 28. 扩展住房土地与城市化模块

- 住房套数是库存，竣工量是年度流量，进入库存前必须除以 12。
- 城市家庭需求直接复用人口家庭账户，不得用城市人口另算一套户数。
- 土地出让收入当前是地方融资备忘项；接入财政时必须同时处理地方债务和土地资产减少，避免重复收入。
- 房价、租金和按揭必须通过收入、信贷和供需传导，不得直接改变 GDP。

## 29. 扩展区域经济模块

- 新增区域必须同步修改 `EconomicRegionId`、配置、空账户、归一化分配、界面名称与守恒测试。
- 区域总量只能分配全国既有总量，不能把六区产出再次相加到全国 GDP。
- 跨区流入对一个地区为正时，必须在其他地区形成等额流出；人口、资本和财政分别核对净额。
- 若让迁移流量改变地区人口，应只改变下一月区域份额，全国人口仍由人口模块唯一决定。

## 30. 扩展世界贸易与金融网络

- 新增世界国家后，旧存档迁移会自动补伙伴账户；不得假设固定伙伴数量。
- 伙伴出口、进口、外资和外债必须分别调和到中国总账户。
- 贸易协定和制裁只能改变伙伴权重及风险；要改变中国贸易总量，应通过既有贸易模块的市场准入机制。
- 新结算币种必须保持每个伙伴币种份额之和为 1，并说明汇率与储备风险承担者。

## 31. 扩展国防战争与安全模块

- 新冲突应有唯一事件 ID、开始结束条件、强度、伤亡率、财政和外汇来源。
- 历史修正器负责实际人口与经济冲击，安全账户负责解释和累计，不得重复扣除。
- 装备、军需库存按月变化；年度预算与采购需求不得整年一次性进入库存。
- 和平路线必须能保持战争成本、战争月数和冲突伤亡为零，并保留正常国防资本折旧与建设。

## 32. 扩展制度执行与因果图

- 新风险必须添加 ID、中文定义、阈值、主要次要驱动、状态初值、界面和测试。
- 风险公式应读取已存在的库存或流量，不得在因果图中复制一套经济模型。
- 预警信号默认只读；若转为真实事件，必须经事件引擎、确定性随机流和 Modifier 系统施加影响。
- 制度库存变化必须渐进。重大改革可改变目标值或调整速度，不应瞬时把所有能力设为满值。

## 33. 不确定性、自动校准与模型完整性

- 新增需要展示区间的最终指标时，同步更新 `UNCERTAINTY_METRIC_IDS`、样本构造、命令行报告和单元测试；不得在汇总器中重新运行或修改模拟。
- 多种子实验必须记录策略、时期和完整种子列表。比较版本时复用同一组种子，不得只挑选有利样本。
- 自动校准参数必须有明确上下界、初值和离散步长。搜索工具只能生成候选，任何配置修改都应作为单独阶段提交，并重新运行留出验证与反事实路线。
- `fit`、`validation`、`projection` 三组不可混用。验证损失恶化的候选默认拒绝，不得以拟合损失下降为由采用。
- 新增守恒账户后，把一个归一化误差指标加入 `evaluateModelIntegrity`；界面和最终审计必须调用同一函数。
- 完整性失败时先检查单位、库存/流量、月度换算和结算顺序，不得通过提高统一容差或 `clamp` 消除错误。

## 34. 维护纯静态部署

- 默认 `npm run build` 必须继续生成 Sites 的 `dist/server/index.js`；纯静态构建只能通过 `npm run build:static` 显式启用，不能永久覆盖默认输出模式。
- 静态导出分支不得调用 `headers()`、`cookies()`、服务端操作或请求期数据。当前元数据只在普通 Sites 构建读取请求主机，静态分支使用根路径图片；新增页面时确保静态分支可以在构建期完整预渲染。
- Web Worker 必须继续使用 `new URL(..., import.meta.url)` 引用，确保打包器生成带内容哈希的独立资源。
- `dist-static/` 是生成物，不提交 Git。发布时上传目录内容本身，不能上传包含它的上级目录。
- 修改入口、资源路径、路由或构建配置后必须运行 `npm run test:static`，核对 `index.html`、`404.html`、资源引用和模拟 Worker。
- 当前静态产物使用根路径资源；需要部署到仓库子路径时，应通过配置增加 `basePath` 并新增子路径访问测试，不得只在托管平台上做字符串替换。
