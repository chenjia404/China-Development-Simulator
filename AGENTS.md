# AGENTS.md

本文件适用于仓库根目录及其全部子目录。后续自动化代理和开发者在修改代码前，应先阅读本文件、`README.md`、`开发文档.md`、`经济模拟算法设计.md` 与 `程序维护与扩展指南.md`。

## 项目目标

本项目是从 1949 年开始、按月推进的中国国家发展模拟器。开发顺序始终坚持：先保证无界面模拟核心正确、确定、可批量运行，再接入 Web Worker 和 React 管理界面。

任何功能都不得以牺牲以下目标为代价：

- 相同种子、相同存档、相同决策必须产生完全一致的结果。
- 1949 年至当前年份必须能够连续运行，不得出现 `NaN`、`Infinity`、负人口或无意义的负产出。
- 国策、外交、财政等决策必须通过中间变量逐步传导，不得直接给 GDP、人口或综合评分加固定值。
- 模拟核心必须独立于 React、DOM、浏览器存储和界面状态。
- 历史校准、极端策略反馈和存档连续性必须持续通过审计。

## 开发环境与语言

- 使用 PowerShell 7，并将终端输入输出设置为 UTF-8。
- 使用 Node.js 22.13.0 或更高版本以及 npm。
- 新增依赖时优先使用最新稳定版本，并同步提交 `package-lock.json`；不得为了方便引入功能重复或维护不明的依赖。
- 代码注释、错误信息、界面文本、提交说明和项目文档统一使用简体中文。
- TypeScript 保持严格类型；避免 `any`、不安全断言和用类型转换掩盖数据结构问题。

## 目录与职责

- `src/simulation/`：无界面模拟核心，不得依赖 React、DOM、`window` 或浏览器 API。
- `src/simulation/core/month-pipeline.ts`：唯一的月度结算顺序入口。新增系统必须明确其库存、流量和结算时点后再接入。
- `src/data/config/`：可校准的数值参数、国策、外交和世界国家配置。可调参数不要散落在组件或业务函数中。
- `src/worker/`：Web Worker 协议与模拟核心桥接层，只负责接收命令和返回结果。
- `src/ui/`：浏览器客户端、Zustand 状态、自动存档等 UI 基础设施。
- `app/`：React 页面和样式。组件只展示状态、收集决策，不得自行计算经济结果。
- `tools/baseline-calibration/`：无界面批量模拟、历史策略和校准工具。
- `tools/final-audit/`：确定性、性能、极端策略、存档连续性和架构边界审计。
- `tests/`：生产构建后的页面输出测试。

## 模拟核心规则

### 确定性

- 模拟逻辑禁止使用 `Math.random()`、当前时间或未序列化的全局状态。
- 所有随机过程必须使用项目提供的可序列化随机数生成器。
- 不同用途的随机流保持隔离；新增随机流时必须写入 `GameState`、存档和迁移逻辑。
- 不得因日志、界面刷新频率或批量执行方式改变模拟结果。

### 时间与数值

- 月度流量与年度比率必须正确换算，避免把年度值每月重复累计。
- 明确区分库存与流量，例如资本存量、债务存量、年度投资和月度新增量。
- 明确区分实际值与名义值；涉及价格指数时不得直接混加不同口径金额。
- 对概率、比例、指数和资源供应率设置有经济含义的边界，但不得用 `clamp` 隐藏公式错误。
- 政策、教育、人口、科研和制度变化应体现过渡期或滞后，不应瞬时跳到目标值。

### 政策与反馈

- 国策定义优先放入 `src/data/config/national-policies.json`，通过统一 modifier 机制作用于中间变量。
- 新国策必须说明生效周期、互斥关系、主要收益和代价；强政策应有资源、财政、通胀、民生或外交方面的取舍。
- 外交行动必须有点数成本、条件或冷却期。贸易协定、战略伙伴和制裁必须对贸易、外资或声誉形成可测试的反馈。
- 财政赤字、债务、货币化融资和通胀必须形成闭环，不得只有收益没有风险。
- 随机事件统一通过 Modifier 系统生效并进入年度报告。

## 状态、命令与存档

- 对模拟状态的外部修改必须通过 `SimulationCommand` 和引擎 `dispatch` 完成。
- 修改 `GameState` 或 `WorldState` 时，同时检查：初始状态、引擎导入、JSON 存档反序列化、Worker 协议、UI 和测试。
- 新增可选字段时为旧存档提供确定的迁移默认值；只有确实无法兼容时才提升存档版本。
- 导出状态必须是深拷贝，不能向调用方泄漏引擎内部可变引用。
- 存档后继续运行的结果必须与不中断运行完全一致。

## React 与交互

- 模拟计算在独立 Web Worker 中执行，主线程不得复制一套模拟公式。
- Zustand 只保存界面和 Worker 返回的状态；异步操作必须处理忙碌状态和中文错误提示。
- 所有重要操作应显示成本、门槛、互斥或不可执行原因。
- 新页面同时检查桌面端和窄屏布局，并保留深色模式、重新开始、导入导出和自动运行功能。
- 图表等大型依赖应继续懒加载，避免扩大首屏包体。

## 校准与真实性

- `src/data/config/calibration-targets.json` 是历史锚点与容差的唯一来源。
- 校准失败时先查公式、单位、结算顺序和策略时间点，不得仅通过放宽容差让测试变绿。
- 新系统接入后，比较变更前后的历史路线结果，解释任何显著偏移。
- 对新增机制至少编写一个正向测试和一个代价或反向反馈测试。
- 当前年份变化时，更新批量模拟、校准目标和审计周期，避免只修改界面年份。

## 验证要求

小范围修改至少运行相关测试和类型检查。影响模拟状态、结算管线、国策、外交、财政或校准时，完成前依次运行：

```powershell
npm run typecheck
npm run lint
npm test
npm run data:audit
npm run calibrate
npm run audit
npm run build
npm run test:ui
```

交付时报告实际通过数量、校准结果和未执行的检查。构建块体积警告可以记录，但不得忽略真正的错误。

## Git 与分阶段工作

- 开始前检查 `git status`，保留用户已有改动，不得擅自覆盖、回退或提交无关文件。
- 多阶段任务必须维护清晰计划，每完成一个可独立验证的阶段就提交一次代码。
- 提交前运行与该阶段风险相称的测试，并用 `git diff --check` 检查空白错误。
- 提交信息使用简体中文，推荐格式为 `阶段N：动作与模块`；单文件文档任务可使用 `新增……说明`。
- 不得使用 `git reset --hard`、`git checkout --` 等破坏性命令清理用户改动。
- `dist/`、`outputs/`、`work/`、本地日志和临时归档属于生成物，不得提交。

## 部署

- 只有用户要求发布或当前任务明确包含部署时才更新线上站点。
- 复用 `.openai/hosting.json` 中已有的 Sites 项目，不得重复创建站点。
- 部署前必须完成生产构建并提交精确源码；部署内容必须对应当前分支的已验证提交。
- 默认保持现有私有访问级别，不得擅自改为公开或扩大访问范围。
- 令牌、源代码仓库临时凭据和部署密钥不得写入文件、Git 配置、远程 URL、日志或回复。
